let num = 20;
let num2 = 30;

const sum = (number, number1) => {
    const a = number + 2;
    const b = number1 + 2;

    const multiply = a * b;
    return multiply;
}

const result = sum(num, num2);
console.log(result);