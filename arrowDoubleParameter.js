let num = 10;
let num1 = 20;

const sum = (number, number1) => {
    const a = number + 2;
    const b = number1 + 2;
    const multiply = a * b;
    return multiply;
}

const result = sum(num, num1);
console.log(result);